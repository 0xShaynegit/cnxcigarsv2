{
  "name": "cnxcigarsv2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html services.html founder.html brands favicon fonts images _headers robots.txt sitemap.xml site.webmanifest dist/ 2>/dev/null || true",
    "deploy": "npm run build && wrangler deploy"
  }
}
